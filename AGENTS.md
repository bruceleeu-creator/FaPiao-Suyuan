# 发票溯源证据链系统项目记忆

## 项目定位

- 本项目不是单纯 OCR 发票识别工具。
- 本项目定位为“以发票为入口、以业务事件为核心、以证据链为基础、以财税风险决策为结果”的智能入账系统。
- 后续所有产品和开发方案都应围绕“事实、证据、规则、风险、凭证草稿”展开。

## 当前项目文件

- 项目根目录：`C:\Users\Administrator\Desktop\开发知识库\FaPiao-Suyuan`（Windows 开发机；生产服务器 49.232.160.7）
- 核心来源文档：`docs/发票入账系统产品架构方案.md`
- 当前文档索引：根目录 4 个活文档（README / AGENTS / agent / progress），详见 `agent.md` 第 7 节与 `README.md` 第 13 节
- 规则文档：`docs/验真与凭证规则设计.md`（验真/凭证规则变更的权威来源）
- 历史过程文档：`docs/archive/202607-process/`（47 篇）
- 当前暂不与 Obsidian 同步，所有产出先放在本项目文件夹。

## 核心产品结构

- 一级功能保持三个板块：发票识别、业务还原、入账风险。
- 发票识别负责票面信息、验真、查重、红冲、异常、候选场景和置信度。
- 业务还原负责追问事实、匹配证据、识别矛盾、形成标准业务事实。
- 入账风险负责会计处理、税务处理、证据缺口、风险卡片、整改要求和凭证草稿。

## 核心对象

- 发票对象：票面字段、验真、查重、红冲、状态、异常和置信度。
- 业务事件对象：人员、对象、时间、地点、目的、项目、付款、受益人和承担理由。
- 证据链对象：合同、订单、付款、报销、审批、出差、会议、拜访、入库、验收等材料。
- 风险决策对象：会计结论、税务结论、内控结论、风险等级、整改、审批和凭证草稿。

## 一期边界

- 一期覆盖八类高频发票：餐饮、住宿、交通、车辆、办公、咨询服务、广告推广、租赁物业。
- 一期只生成凭证草稿，不自动过账。
- 一期不做自动申报、不做完整纳税申报表、不做全量合同系统、不做全量银行流水系统。
- 高风险、低置信度、重大金额、关联交易、证据缺失必须人工确认。

## 已确认首期决策

- 首期路线选择 B：业务闭环 MVP 优先。
- 产品名称统一为“发票溯源证据链系统”。
- 一期只做电脑端，手机端仅预留入口和未来扩展位置。
- 一期不开发手机端财务审核。
- 一期采用“八类覆盖、三类做深”。
- 三类做深为餐饮、住宿、咨询服务。
- 第一版 OCR、验真、凭证接口先用模拟数据跑通，后续再替换真实接口。
- 进入正式开发前先初始化 Git 版本管理并完成 GitHub 备份。

## 端侧定位

- 一期电脑端优先，覆盖经办、财务和管理的核心流程。
- 手机端一期只保留入口占位，不作为验收主功能。
- 财务端优先做 Web 工作台：异常队列、三阶段审核、风险卡片、凭证草稿和人工修正。
- 管理端优先做 Web 驾驶舱：风险结构、部门人员供应商风险、AI采纳率、误报率和规则阈值。

## AI 与规则边界

- AI负责语义理解、场景推理、动态追问、证据匹配、矛盾识别、解释和报告生成。
- 规则引擎负责确定性判断：金额税额、税率、查重、时限、比例限额、审批阈值、费用标准和科目映射。
- 资料不足时输出“暂不能判断”，不得强行形成唯一结论。

## 开发注意事项

- 先落地可运行闭环，再扩展规则和税种。
- 先做模拟接口跑通，再替换真实 OCR、验真、凭证、ERP 或报销系统接口。
- 开发前应先初始化 Git 或确认远端备份策略。
- 后续如果进入编码阶段，应同时建设演示案例、规则测试、端到端验收和操作日志。

## TRAE 督导机制

- T 指 TRAE Work CN，负责实际开发执行。
- Codex 负责技术支持、质量督导、验收和版本管理。
- 每次 T 工作完成后，Codex 必须自动复核，不需要用户提醒。
- 自动复核包括：查看改动范围、运行测试、运行构建、访问本地预览、对照 Gate 验收清单检查应用需求。
- 如果发现缺口，Codex 直接在 T 中下发整改指令。
- 只有复核通过后，Codex 才能更新验收记录、提交 Git 并推送 GitHub。
- 未经 Codex 复核通过的 T 产出不得视为阶段完成。
