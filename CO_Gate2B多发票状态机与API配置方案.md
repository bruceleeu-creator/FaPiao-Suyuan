# CO_Gate2B多发票状态机与API配置方案

> 日期：2026-07-17
> 项目：发票溯源证据链系统
> 阶段：Gate 2B

## 一、统一推进建议

Gate 2A 已经证明单张发票可以从录入跑到风险建议。下一步统一进入：

> Gate 2B：多发票流程管理、16 状态机、操作日志持久化、异常队列联动、正式 API 配置中心。

本轮目标不是接入真实外部接口，而是把未来真实接入所需的配置、模式、状态和日志底座先搭好。

## 二、本轮必须解决的问题

### 1. 多发票会话

当前流程使用 `/invoices/current/...`，只适合单条演示。

Gate 2B 要求：

- 每次录入生成独立 `caseId`。
- 发票列表展示多条发票。
- 每条发票有自己的状态、操作日志、AI 结果和风险建议。
- 支持从列表继续处理某一张发票。

### 2. 16 状态机

把现有流程状态升级为明确状态机，覆盖：

1. 待识别
2. 识别中
3. 待确认票面
4. 待还原业务
5. 待回答问题
6. 待补充证据
7. 业务已还原
8. 待风险判断
9. 待财务复核
10. 待负责人审批
11. 待生成凭证
12. 凭证草稿已生成
13. 已完成
14. 已退回
15. 暂不能判断
16. 已作废或已红冲

要求：

- 每次状态流转必须记录日志。
- 不允许跳过必要状态直接生成凭证草稿。
- 高风险、证据缺失、业务事实不足必须进入复核、补证据或暂不能判断。

### 3. 操作日志持久化

当前日志存在内存中，刷新后丢失。

Gate 2B 先使用 `localStorage`，不引入后端数据库。

必须持久化：

- 发票对象
- 当前流程状态
- 业务事件对象
- 证据链对象
- AI 介入记录
- 风险建议草稿
- 操作日志

### 4. 异常队列联动

异常工作台不能只展示固定 demo 数据。

Gate 2B 要求异常队列能读取真实本地流程数据：

- 验真失败
- 疑似重复
- 红冲/作废
- 证据缺失
- 业务追问不足
- 低置信度
- 高风险
- 暂不能判断

用户可以从异常队列继续处理对应发票。

### 5. 正式 API 配置中心

当前侧边栏只有接口边界展示，没有正式配置功能。

Gate 2B 要求新增 `接口配置` 页面，路径建议：`/settings/integrations`。

配置对象至少包括：

1. OCR 识别 API
2. 发票验真 API
3. 凭证接口 API

每个接口配置字段：

- 接口模式：`模拟` / `正式`
- Base URL
- API Key 或 Token，占位输入，不需要真实密钥
- 超时时间
- 重试次数
- 启用状态
- 最后一次测试结果

配置行为：

- 可以保存到 `localStorage`。
- 可以点击 `测试连接`。
- 当前阶段测试连接使用模拟结果，不发真实外部请求。
- 如果模式切换到 `正式`，页面必须提示：`当前仅保存配置，不调用真实接口`。
- 侧边栏 `接口边界` 区域应能显示当前模式，例如 `OCR 识别：模拟/正式配置未启用`。

## 三、禁止事项

本轮仍然禁止：

1. 不真实调用 OCR。
2. 不真实调用发票验真。
3. 不真实调用凭证接口。
4. 不自动过账。
5. 不自动申报。
6. 不做完整手机端。
7. 不引入重型后端或数据库。
8. 不删除现有方案文档。

## 四、建议实现方式

### 数据层

新增本地 store：

- `src/storage/localStore.ts`
- `src/cases/caseStore.ts`
- `src/integrations/integrationConfigStore.ts`

建议 localStorage key：

- `invoice_evidence_cases`
- `invoice_evidence_active_case_id`
- `invoice_evidence_integration_config`

### 状态机

新增：

- `src/workflow/statusMachine.ts`

内容包括：

- 状态枚举复用 `WorkflowStatus`
- 合法流转表
- `canTransition(from, to)`
- `transitionCase(caseId, action)`
- 状态流转日志生成

### API 配置

新增：

- `src/integrations/types.ts`
- `src/integrations/defaultConfigs.ts`
- `src/integrations/mockConnectionTest.ts`
- `src/ui/pages/IntegrationSettingsPage.tsx`

## 五、验收标准

Gate 2B 通过条件：

1. 可以录入至少 2 张发票，并在列表中看到不同状态。
2. 刷新页面后，发票、状态、日志和配置不丢失。
3. 从发票列表可继续处理任一发票。
4. 异常工作台能显示本地流程产生的异常发票。
5. 16 状态机有测试覆盖，非法流转会被拒绝。
6. 操作日志持久化，有测试覆盖。
7. `接口配置` 页面可配置 OCR、验真、凭证接口。
8. API 配置可保存、刷新后可恢复。
9. `测试连接` 使用模拟结果，不发真实请求。
10. `模拟/正式` 模式可切换，但正式模式不触发真实调用。
11. `npm test`、`npm run build`、`npm audit --omit=dev` 通过。

## 六、交付后 Codex 复核重点

Codex 将按以下路径复核：

1. 录入第一张正常餐饮发票。
2. 录入第二张高风险或证据缺失发票。
3. 刷新页面，确认两张发票仍存在。
4. 从列表继续处理其中一张。
5. 进入异常工作台，确认异常发票自动出现。
6. 进入接口配置页，配置 OCR / 验真 / 凭证接口。
7. 切换模拟/正式模式，保存并刷新确认配置不丢失。
8. 点击测试连接，确认只是模拟测试。
9. 运行测试、构建和安全审计。

## 七、下一阶段预告

Gate 2B 通过后，再进入 Gate 3：四道闸门规则模块化、更多异常案例和规则版本管理。
