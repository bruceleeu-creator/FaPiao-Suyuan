# Dependencies
node_modules/

# Build output
dist/
build/
coverage/
playwright-report/
test-results/

# TypeScript incremental build artifacts
.tsbuild/
*.tsbuildinfo

# Environment
.env
.env.local
.env.*.local

# System files
.DS_Store

# Logs
*.log
npm-debug.log*
yarn-debug.log*
yarn-error.log*

# Local encrypted Tencent credentials (never commit)
server/.tencent-credential-key
server/.tencent-credentials.enc

# Account database (users.json / auth.secret, never commit)
server/data/

# DeepSeek key encrypted store (never commit)
server/.deepseek-credentials.enc

# Mimosa scanner runtime state (never commit)
.mimosa/

# Playwright MCP 浏览器测试产物
.playwright-mcp/
