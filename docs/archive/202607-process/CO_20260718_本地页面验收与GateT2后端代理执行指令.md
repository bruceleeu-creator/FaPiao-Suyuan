# CO_20260718 本地页面验收与 Gate T2 后端代理执行指令

执行对象：TRAE Work CN

督导角色：Codex

项目路径：`/Users/yfk009/Documents/AI项目库/发票溯源证据链系统`

当前 GitHub 备份基线：`247e272 feat: reserve tencent invoice integrations`

## 1. 本地页面验收结果

Codex 已启动本地应用并使用真实浏览器做页面验收：

- 本地地址：`http://127.0.0.1:53173/`
- 验收截图目录：`output/playwright/20260718-gate-t2-intake/`
- 验收报告：`output/playwright/20260718-gate-t2-intake/page-acceptance-report.json`

已通过项：

- 首页左下角显示“接口边界”。
- “接口配置”和“手机端入口”已统一放入左下角“接口边界”。
- 首页不再出现旧的接口配置快捷卡。
- 接口配置页显示“腾讯云接口预留状态”。
- 接口配置页没有 API Key / Token 密码输入框。
- 接口配置页明确提示前端不保存密钥，真实密钥未来由后端代理和环境变量管理。
- 发票录入页显示腾讯云 OCR 预留提示，且不要求用户处理密钥。
- 手机端入口页可以打开，并保持一期占位边界。
- 移动视口首页可以正常渲染。
- 浏览器控制台未发现运行时错误。

## 2. 本地页面验收发现的整改项

### P1：演示样例无法直达新版决策结果和 CSV 导出入口

现象：

- 访问 `/invoices/case-catering-001/decision` 或 `/invoices/current/decision` 时，页面显示“尚未生成风险建议”。
- 因为演示样例没有进入本地 `WorkflowSession`，`DecisionResultPage` 只找本地 workflow session，无法读取 `demoCases` 里的 `riskDecision`。
- 这会导致部门演示时看不到新版“导出标准凭证 CSV”入口，容易误判为功能没有完成。

整改要求：

1. 支持演示样例在决策结果页直接展示。
2. 最低要求：访问 `/invoices/case-catering-001/decision` 时，能够显示演示样例的风险决策、凭证草稿和标准凭证 CSV 导出入口。
3. `InvoiceListPage` 中演示样例的“查看演示”应能让用户清晰进入结果或详情，不要进入一个看不到新版能力的空状态。
4. 建议实现方式：
   - 在 `DecisionResultPage` 中，当本地 session 不存在时，尝试从 `demoCases` 按 `caseId` 查找演示样例。
   - 增加一个小型适配函数，将 `DemoCase.riskDecision` 转成 `DecisionDraft` 需要的字段。
   - 不要为了查看演示而污染本地 `localStorage`。
5. 验收要求：
   - `/invoices/case-catering-001/decision` 显示“导出标准凭证 CSV”。
   - 点击低风险演示样例的 CSV 按钮可以下载 `voucher-YYYYMMDD-10012001-draft.csv`。
   - 高风险/验真失败/疑似重复演示样例必须保持阻断，不得导出可导入凭证。
   - 增加或更新测试覆盖演示样例结果页可达性或适配逻辑。

## 3. Gate T2 开发目标

在不调用真实腾讯云服务、不保存真实密钥的前提下，搭建后端代理骨架，为后续真实接入腾讯云 OCR 和腾讯云发票核验做准备。

当前项目是 React + Vite 前端应用，没有后端框架。Gate T2 建议使用 Node 原生 HTTP 服务实现最小后端代理骨架，避免引入 Express/Fastify 等新依赖。

## 4. Gate T2 后端代理骨架范围

新增建议目录：

- `server/`
- `server/tencentProxyServer.mjs`
- `server/tencentProxyResponses.mjs`
- `server/tencentProxyConfig.mjs`
- `server/README.md`
- `scripts/backend-smoke.mjs`
- `.env.example`

新增或更新 `package.json` scripts：

- `backend:dev`: 启动本地后端代理，例如 `node server/tencentProxyServer.mjs`
- `backend:smoke`: 启动临时服务并检查核心接口返回
- `validate`: 统一跑 `npm test`、`npm run build`、`npm run backend:smoke`

建议端口：

- 后端代理默认 `127.0.0.1:8787`
- 可通过 `BACKEND_PROXY_PORT` 覆盖

## 5. 后端接口要求

### GET `/health`

返回示例：

```json
{
  "ok": true,
  "service": "invoice-evidence-backend-proxy",
  "mode": "reserved",
  "tencent": {
    "ocrConfigured": false,
    "verifyConfigured": false
  }
}
```

### POST `/api/tencent/ocr/invoice`

当前只返回预留结果，不调用腾讯云：

```json
{
  "ok": false,
  "provider": "tencent-cloud",
  "service": "invoice-ocr",
  "status": "reserved",
  "reserved": true,
  "simulated": true,
  "message": "腾讯云 OCR 服务尚未开通，后端代理入口已预留。",
  "traceId": "..."
}
```

### POST `/api/tencent/invoice/verify`

当前只返回预留结果，不调用腾讯云：

```json
{
  "ok": false,
  "provider": "tencent-cloud",
  "service": "invoice-verify",
  "status": "reserved",
  "reserved": true,
  "simulated": true,
  "message": "腾讯云发票核验服务尚未开通，后端代理入口已预留。",
  "traceId": "..."
}
```

### 错误格式

所有错误统一返回：

```json
{
  "ok": false,
  "errorCode": "BAD_REQUEST",
  "message": "请求体不是合法 JSON。",
  "traceId": "..."
}
```

## 6. 安全边界

必须遵守：

- 不调用真实腾讯云接口。
- 不在前端源码、测试、文档示例、localStorage 中保存真实密钥。
- `.env.example` 只能写变量名，不能写真实值。
- 允许出现的变量名：
  - `TENCENT_CLOUD_SECRET_ID=`
  - `TENCENT_CLOUD_SECRET_KEY=`
  - `TENCENT_CLOUD_REGION=ap-guangzhou`
  - `BACKEND_PROXY_PORT=8787`
- 后端启动时只检查变量是否存在，不打印变量值。
- 日志中不得输出请求原文中的敏感字段。
- CORS 当前只允许本地开发源，例如 `http://127.0.0.1:5173`、`http://127.0.0.1:53173`。

## 7. 测试和验收要求

TRAE 完成后必须运行并报告：

```bash
git diff --check
npm test
npm run build
npm run backend:smoke
npm run validate
npm audit --omit=dev
```

额外源码扫描要求：

```bash
rg -n "SecretId=|SecretKey=|TENCENT_CLOUD_SECRET_ID=.*[^=]$|TENCENT_CLOUD_SECRET_KEY=.*[^=]$|ocr.tencentcloudapi.com|invoice.tencentcloudapi.com|fetch\\(" src server scripts .env.example
```

说明：

- `fetch(` 如果只出现在 `scripts/backend-smoke.mjs` 调本地 `127.0.0.1:8787`，可以接受。
- 真实腾讯云域名不应出现在会执行请求的代码里；如必须出现在文档，应说明“后续真实接入参考，不在 Gate T2 调用”。

## 8. 交付边界

TRAE 不要提交 Git。

完成后只输出：

- 修改文件清单。
- P1 页面整改说明。
- Gate T2 后端代理骨架说明。
- 所有命令的结果。
- 仍未完成的真实接入前置条件。

Codex 会在 TRAE 完成后再次做质量督导复核，复核通过后再提交并推送 GitHub。

